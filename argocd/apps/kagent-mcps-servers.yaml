apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kagent-mcps-servers
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: default
  source:
    repoURL: https://github.com/k0rvih/course-aire
    targetRevision: main
    path: argocd/templates/kagent
    directory:
      recurse: true
      include: "*.yaml"
  destination:
    server: https://kubernetes.default.svc
    namespace: kagent
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
